# AGENTS.md

The project source, build tooling, tests, and docs live in [`V0/`](./V0/).

**Start there:** read [`V0/AGENTS.md`](./V0/AGENTS.md) for the full agent and
development instructions. Run build, lint, and test commands from inside `V0/`
(for example `cd V0 && make test`).

The repository root is a thin landing layer — see [`README.md`](./README.md).
